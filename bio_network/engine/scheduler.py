"""Event-driven scheduler that advances the network on spikes."""

# TODO(M1): implement an event scheduler that advances simulation time only
#           when spikes occur, instead of stepping every neuron per tick.